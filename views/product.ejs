<!DOCTYPE html>
<html lang="fr">
  <head>
    <%- include('./partials/header') %> 
  </head>
  <body>
    <main class="container mt-5">
      <div class="row">
        <div class="col-12">
          <img
            src="/assets/coffees/<%= parseInt(coffee.reference) %>.png"
            alt="<%= coffee.name %>"
            class="img-fluid rounded mx-auto d-block product-image mb-5"
          />
        </div>
      </div>

      <div class="row">
          <div class="col-9 product-info">
            <div class="card h-100">
              <div class="card-body d-flex flex-column">
                <h2><%= coffee.name %></h2>
                <p class="text-muted">Publié le 21/02/2025</p>
                <p>Description : <%= coffee.description %></p>
                <p>Référence : <%= coffee.reference %></p>
                <p>Origine : <%= coffee.origin %></p>
                <p>Prix au kilo : <%= coffee.price %> €</p>
                <p>Caractéristique principale : <%= coffee.category %></p>
                <p>Disponible : <% if (coffee.availability === true) { %>
                 Oui
                <% } else if (coffee.availability === false) { %>
                  Non
                 <% }%></p>
                <div class="mt-4">
                  <a href="/catalog" class="btn btn-primary">Catalogue</a>
                </div>
              </div>
            </div>
          </div>
      </div>
    </main>

    <%- include('./partials/footer') %> 
  </body>
</html>
